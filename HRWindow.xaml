<Window x:Class="ST10446806_PROG6212_POEPART1.HRWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="HR Window" Height="450" Width="800" Background="#D2DFEF">
    <Grid Margin="10">

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*"/>
            <ColumnDefinition Width="3*"/>
        </Grid.ColumnDefinitions>

        <!-- Lecturer List -->
        <GroupBox Header="Lecturers" Grid.Column="0" Margin="5">
            <ListBox Name="LecturerListBox" DisplayMemberPath="FullName" SelectionChanged="LecturerListBox_SelectionChanged" Background="#E0EAFF"/>
        </GroupBox>

        <!-- Lecturer Details -->
        <GroupBox Header="Lecturer Details" Grid.Column="1" Margin="5" Background="#D2DFEF">
            <StackPanel Margin="10">
                <TextBlock Text="Full Name"/>
                <TextBox Name="FullNameTextBox" Margin="0,5,0,10" Background="#E0EAFF"/>

                <TextBlock Text="Email"/>
                <TextBox Name="EmailTextBox" Margin="0,5,0,10" Background="#E0EAFF"/>

                <TextBlock Text="Phone Number"/>
                <TextBox Name="PhoneTextBox" Margin="0,5,0,10" Background="#E0EAFF"/>

              

                <TextBlock Text="Bank Details"/>
                <TextBox Name="BankDetailsTextBox" Margin="0,5,0,10" Background="#E0EAFF"/>

                <Button Name="SaveButton" Content="Save" Width="100" Click="SaveButton_Click" Background="#D2DFEF"/>

                <Button Content="Generate Invoice Report"
        Width="200"
        Margin="10"
        Click="GenerateInvoice_Click" Background="#D2DFEF"/>

            </StackPanel>
        </GroupBox>

    </Grid>
</Window>
